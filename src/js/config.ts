export default  {
    
    /** Time between steps. */
    duration: 6000,

    /** The available sides. */
    sides: ['left', 'right'],

    /** The available limbs. */
    limbs: ['hand', 'foot'],

    /** The available colors. */
    colors: ['red', 'blue', 'green', 'purple']
}